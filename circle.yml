version: 2.1
jobs:
  test:
    docker:
    - image: circleci/node
    steps:
    - checkout
    - run:
        name: Test
        command: yarn test
workflows:
  version: 2.1
  test:
    jobs:
    - test

